# 🚀 Быстрый деплой на Vercel

## Шаг 1: Установка зависимостей

```bash
npm install
```

## Шаг 2: Деплой через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в аккаунт
vercel login

# Деплой
vercel
```

Следуйте инструкциям в терминале. При первом деплое Vercel спросит:
- Link to existing project? → **No** (для нового проекта)
- Project name → введите название или нажмите Enter
- Directory → **./** (корень проекта)
- Override settings? → **No**

## Шаг 3: Настройка Vercel KV (опционально, но рекомендуется)

Для сохранения данных между перезапусками:

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Storage** → **Create Database**
4. Выберите **KV** (Key-Value)
5. Создайте базу данных
6. Переменные окружения настроятся автоматически

**Без KV:** Данные будут храниться в памяти и сбрасываться при каждом перезапуске функции.

## Шаг 4: Проверка деплоя

После деплоя откройте URL, который предоставит Vercel (например: `https://your-project.vercel.app`)

## Локальная разработка с Vercel функциями

Для тестирования serverless функций локально:

```bash
vercel dev
```

Это запустит локальный сервер с поддержкой Vercel функций на `http://localhost:3000`

## Структура API

Все API endpoints доступны по пути `/api/*`:

- `GET /api/state` - получить состояние дашборда
- `POST /api/state` - сохранить состояние
- `GET /api/roadmap` - получить все элементы roadmap
- `GET /api/roadmap?quarter=Q1` - получить элементы по кварталу
- `POST /api/roadmap` - создать элемент
- `PUT /api/roadmap/[id]` - обновить элемент
- `DELETE /api/roadmap/[id]` - удалить элемент
- `POST /api/roadmap/[id]/toggle` - переключить статус
- `GET /api/export` - экспортировать данные
- `POST /api/reset` - сбросить к значениям по умолчанию

## Troubleshooting

### Ошибка сборки

Убедитесь, что:
- Все зависимости установлены (`npm install`)
- `package.json` содержит правильный скрипт `build`
- `vite.config.js` настроен правильно

### API не работает

- Проверьте логи в Vercel Dashboard → Functions
- Убедитесь, что файлы в папке `api/` используют правильный синтаксис экспорта
- Проверьте CORS заголовки в функциях

### Данные не сохраняются

- Настройте Vercel KV (см. Шаг 3)
- Или используйте внешнюю базу данных

## Полезные команды

```bash
# Просмотр логов
vercel logs

# Просмотр переменных окружения
vercel env ls

# Локальная разработка
vercel dev

# Production деплой
vercel --prod
```
