# Алсофт Dashboard

Интерактивный продуктовый дашборд для управления бизнес-метриками с бекендом и базой данных.

## Возможности

- 📊 Отслеживание прогресса и метрик
- 💰 Финансовое планирование и анализ
- 🚀 Roadmap продукта с отслеживанием фич
- ⚙️ Настройки и управление данными
- 💾 Автоматическое сохранение в базу данных

## Технологии

- **Frontend**: HTML, CSS, JavaScript, Chart.js
- **Backend**: Node.js, Express
- **Database**: SQLite (better-sqlite3)

## Установка

1. Установите зависимости:
```bash
npm install
```

2. Запустите сервер:
```bash
npm start
```

Для разработки с автоперезагрузкой:
```bash
npm run dev
```

3. Откройте браузер:
```
http://localhost:3000/index.html
```

## API Endpoints

- `GET /api/state` - Получить текущее состояние дашборда
- `POST /api/state` - Сохранить состояние дашборда
- `GET /api/export` - Экспортировать данные в JSON
- `POST /api/reset` - Сбросить данные к значениям по умолчанию
- `GET /health` - Проверка здоровья сервера

## Структура проекта

```
ALSoft/
├── index.html          # Фронтенд приложения
├── server.js           # Основной файл сервера
├── database.js         # Настройка базы данных
├── routes/
│   └── api.js          # API endpoints
├── package.json        # Зависимости проекта
├── dashboard.db        # SQLite база данных (создается автоматически)
└── README.md           # Документация
```

## База данных

База данных SQLite создается автоматически при первом запуске. Таблица `dashboard_state` хранит все параметры дашборда:

- `currentCourts` - Текущее количество кортов
- `targetCourts` - Целевое количество кортов
- `occupancyRate` - Загрузка слотов (%)
- `conversionRate` - Конверсия (%)
- `capexEquipment` - CAPEX оборудование
- `capexInstall` - CAPEX установка
- `mrr` - Monthly Recurring Revenue
- `opex` - Операционные расходы
- `features` - JSON с состоянием фич

## Разработка

Для разработки рекомендуется использовать `nodemon` для автоматической перезагрузки сервера при изменениях:

```bash
npm run dev
```

## Лицензия

ISC